#include <stdio.h>
int main(){
int i,j;
    for(i=1;i<=5;i++)
    {
        for(j=1;j<=i;j++)
        {
            printf("* "); //just like above we just have to do this, just use the star to print these things.
        }
        printf("\n");
    }    
return 0;
}